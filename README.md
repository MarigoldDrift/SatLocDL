A script that downloads satellite location data from satelliteweb.space.

Requirements:
  -selenium
  -pandas


Use arguments to adjust the amount of data, intervals, and coordinates of the satellite data.
  --count 
  --interval
  --coordinates
